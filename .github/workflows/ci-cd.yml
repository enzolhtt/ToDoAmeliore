name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_TEST_KEY }}

      - name: Add VPS host to known_hosts
        run: |
          ssh-keyscan ${{ secrets.SFTP_SERVER }} >> ~/.ssh/known_hosts

      - name: Copy project files to VPS
        run: |
          rsync -avz --delete ./ ${{ secrets.SFTP_USERNAME }}@${{ secrets.SFTP_SERVER }}:/home/${{ secrets.SFTP_USERNAME }}/groupe4/

      - name: Run deploy script on VPS
        run: |
          ssh ${{ secrets.SFTP_USERNAME }}@${{ secrets.SFTP_SERVER }} "cd /home/${{ secrets.SFTP_USERNAME }}/groupe4 && bash deploy.sh"
